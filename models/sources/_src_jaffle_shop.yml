version: 2 
# source tables from dbt Mesh
sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    description: E-commerce data
    tables:
      - name: customers
        description: One record per person who has purchased one or more items
      - name: orders
        description: One record per order (consisting of one or more order items)
      - name: items
        description: Items included in an order
      - name: stores
      - name: products
        description: One record per SKU for items sold in stores
      - name: supplies
        description: One record per supply per SKU of items sold in stores


# source tables from dbt Fnudamentals course

# sources:
#   - name: jaffle_shop
#     database: raw
#     schema: jaffle_shop

#     tables:
#       - name: customers
#         description: customer info
#         columns:
#           - name: id
#             data_tests:
#               - unique
#               - not_null
#       - name: orders
#         description: orders with financial info
#         columns:
#           - name: id
#             data_tests:
#               - unique
#               - not_null
#           - name: status
#             data_tests:
#               - accepted_values:
#                   arguments:
#                     values: ['completed', 'shipped', 'returned', 'placed', 'return_pending']
#         config:
#           freshness:
#             warn_after:
#               count: 24
#               period: hour
#             error_after:
#               count: 48
#               period: hour
#           loaded_at_field: _etl_loaded_at